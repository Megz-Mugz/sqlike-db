CREATE statements


CREATE TABLE identifier;

or

CREATE TABLE table_name (
  identifier datatype constraint,
);

datatype -> int, boolean, text

constraint -> PRIMARY KEY, NOT NULL, DEFAULT, UNIQUE, FOREIGN KEY,

- note, for purposes of this db, "DEFAULT" will make ints 0, 
booleans false, and text an empty string

SELECT statements


UPDATE statements


DELETE statements